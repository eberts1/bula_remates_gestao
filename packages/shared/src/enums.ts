export enum TenantRole {
  OWNER = 'owner',
  ADMIN = 'admin',
  MEMBER = 'member',
  VIEWER = 'viewer',
}

export enum DocumentStatus {
  DRAFT = 'draft',
  READY = 'ready',
  DELETED = 'deleted',
}
